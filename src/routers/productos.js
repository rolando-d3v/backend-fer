const {Router} = require('express');
const {verificaToken} = require('../middlewares/auth')


const {verificaToken} = require('../controllers/productosController')

const router = Router()

module.exports = router

